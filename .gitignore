# ── Private admin tools — NEVER upload these ──
admin.html
fetch/

# ── API credentials — NEVER commit these ──
.env
*.env
config.local.js
secrets.js
credentials.js
credentials.json
api-keys.js
*.key

# ── OS / editor junk ──
.DS_Store
Thumbs.db
desktop.ini
*.log

# ── VS Code ──
.vscode/settings.json
.vscode/launch.json

# ── Local data overrides ──
data/local/
*.local.json

# ── Temp / backup files ──
*.tmp
*.bak
*.orig
*~

# ── Node (if you ever use it) ──
node_modules/
package-lock.json

# ── Build output ──
dist/
build/

# ── Test/dev files ──
test.html
dev.html
draft-*.html
_old/
_backup/
